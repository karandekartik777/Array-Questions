# Input: arr[] = {1, 1, 2, 2, 2, 2, 3,},   x = 2
#   Output: 4 // x (or 2) occurs 4 times in arr[]

def countfreq(arr, ele):
    count = 0
    for i in range(len(arr)):
        if arr[i]==ele:
            count +=1
    return count
      
arr = [1, 1, 2, 2, 2, 2, 3]      
print(countfreq(arr,2))
